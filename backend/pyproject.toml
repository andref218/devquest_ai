[project]
name = "devquest-ai-backend"
version = "0.1.0"
description = "Backend API for the DevQuest AI multi-agent system."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.141.1",
    "ollama>=0.6.2",
    "uvicorn>=0.52.1",
]
